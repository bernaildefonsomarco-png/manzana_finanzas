import { PlaceholderSection } from "@/shared/placeholder-section";

// Doc `34`, dueño `W-13`.
export default function AsistentePage() {
  return (
    <PlaceholderSection
      title="Asistente"
      description="El asistente conversacional llega en un corte posterior."
      backHref="/inicio"
      backLabel="Volver al inicio"
    />
  );
}
