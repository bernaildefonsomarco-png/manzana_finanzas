// `44` §5 ruta 2: crear cuenta → "Tienes S/X". W-08.
import { test } from "@playwright/test";

test.fixme("crear una cuenta muestra el saldo declarado", async () => {
  // Pendiente: implementar cuando exista /mi-dinero (W-08).
});
