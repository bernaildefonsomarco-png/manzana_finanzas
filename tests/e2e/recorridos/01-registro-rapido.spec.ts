// `44` §5 ruta 1: registro rápido → movimiento guardado. Se implementa
// cuando W-07 (rutas) y W-09 (movimientos) estén listos.
import { test } from "@playwright/test";

test.fixme(
  "registro rápido guarda un movimiento visible en el listado",
  async () => {
    // Pendiente: implementar cuando existan /movimientos/nuevo (W-09) y el
    // esqueleto de rutas (W-07).
  }
);
