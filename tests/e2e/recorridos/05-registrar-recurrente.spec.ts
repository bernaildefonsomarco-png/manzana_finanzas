// `44` §5 ruta 5: registrar recurrente → qué pagos vienen. W-11.
import { test } from "@playwright/test";

test.fixme("registrar un recurrente lo muestra en pagos que vienen", async () => {
  // Pendiente: implementar cuando exista /pagos-que-vienen (W-11).
});
