// `44` §5 ruta 6: conectar buzón → pendientes sin registrar nada solo. W-10.
import { test } from "@playwright/test";

test.fixme("conectar el buzón crea pendientes sin registrar movimientos solo", async () => {
  // Pendiente: implementar cuando existan /pendientes y /configuracion/correo (W-10).
});
