// `44` §5 ruta 7: crear presupuesto + 3 gastos → avance. W-12.
import { test } from "@playwright/test";

test.fixme("un presupuesto con tres gastos muestra su avance", async () => {
  // Pendiente: implementar cuando exista /presupuestos (W-12).
});
