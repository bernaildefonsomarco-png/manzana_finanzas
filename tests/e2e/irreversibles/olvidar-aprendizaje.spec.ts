// `51` §6.3: flujo irreversible — olvidar un aprendizaje. W-13.
import { test } from "@playwright/test";

test.fixme("olvidar un aprendizaje deja lápida y no se vuelve a aprender solo", async () => {
  // Pendiente: implementar cuando exista /configuracion/memoria (W-13).
});
