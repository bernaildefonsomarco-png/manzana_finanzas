// `51` §6.3: flujo irreversible — eliminar y restaurar un movimiento. W-09.
import { test } from "@playwright/test";

test.fixme("eliminar un movimiento y restaurarlo lo devuelve intacto", async () => {
  // Pendiente: implementar cuando exista /movimientos/[id] con eliminar/restaurar (W-09).
});
