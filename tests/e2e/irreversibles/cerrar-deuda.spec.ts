// `51` §6.3: flujo irreversible — cerrar una deuda. W-11.
import { test } from "@playwright/test";

test.fixme("cerrar una deuda la saca de las activas de forma irreversible", async () => {
  // Pendiente: implementar cuando exista /deudas/[id] con cierre (W-11).
});
